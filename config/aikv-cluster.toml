# AiKv 集群模式配置模板
# Cluster Mode Configuration Template
#
# 使用方法 / Usage:
#   1. 编译带集群特性的版本 / Build with cluster feature:
#      cargo build --release --features cluster
#
#   2. 复制并修改配置 / Copy and modify config:
#      cp config/aikv-cluster.toml config.toml
#      vim config.toml
#
#   3. 启动服务 / Start service:
#      ./target/release/aikv --config config.toml
#
# 配置状态说明 / Configuration Status Legend:
#   ✅ 已实现 / Implemented - 该配置项已在代码中生效
#   🚧 计划中 / Planned - 该配置项已定义但尚未实现
#

# ============================================================
# 服务器配置 / Server Configuration
# ============================================================
[server]
# ✅ 监听地址（集群模式推荐使用 0.0.0.0）
# Bind address (0.0.0.0 recommended for cluster mode)
host = "0.0.0.0"

# ✅ 数据端口 / Data port
port = 6379

# 🚧 以下配置项尚未实现 / The following options are not yet implemented:
# cluster_port = 16379         # 集群总线端口 / Cluster bus port
# max_connections = 10000      # 最大并发连接数 / Maximum concurrent connections
# connection_timeout = 300     # 连接超时时间（秒）/ Connection timeout in seconds
# tcp_buffer_size = 65536      # TCP 缓冲区大小 / TCP buffer size

# ============================================================
# 集群配置 / Cluster Configuration
# ============================================================
[cluster]
# ✅ 启用集群模式 / Enable cluster mode
enabled = true

# ✅ Raft 通信地址（gRPC）/ Raft communication address (gRPC)
# 每个节点必须使用不同的端口 / Each node must use a different port
raft_address = "127.0.0.1:50051"

# ✅ 是否为引导节点（第一个启动的节点设为 true）
# Is this the bootstrap node (set to true for the first node)
is_bootstrap = false

# 🚧 以下配置项尚未实现 / The following options are not yet implemented:

# ============================================================
# 存储配置 / Storage Configuration
# ============================================================
[storage]
# ✅ 存储引擎类型（集群模式推荐使用 aidb）
# Storage engine type (aidb recommended for cluster mode)
engine = "aidb"

# ✅ 数据存储目录 / Data directory
data_dir = "./data"

# ✅ 数据库数量 / Number of databases
databases = 16

# 🚧 以下配置项尚未实现 / The following options are not yet implemented:
# max_memory = "2GB"           # 最大内存使用 / Maximum memory usage

# ============================================================
# 日志配置 / Logging Configuration
# ============================================================
[logging]
# ✅ 日志级别 / Log level
level = "info"

# 🚧 以下配置项尚未实现 / The following options are not yet implemented:
# file = "./logs/aikv-cluster.log"  # 日志文件路径 / Log file path
# console = true                     # 是否输出到控制台 / Output to console
# max_size = "100MB"                 # 日志轮转大小 / Log rotation size
# max_backups = 10                   # 保留日志文件数量 / Number of backup log files

# ============================================================
# 🚧 以下配置节尚未实现 / The following sections are not yet implemented
# ============================================================

# [raft]
# # Raft 共识配置 / Raft Consensus Configuration
# heartbeat_interval = 100      # 心跳间隔（毫秒）/ Heartbeat interval in milliseconds
# election_timeout_min = 300    # 选举超时范围（毫秒）/ Election timeout range
# election_timeout_max = 500
# snapshot_interval = 10000     # 快照间隔（日志条目数）/ Snapshot interval
# log_compaction_threshold = 50000  # 日志压缩阈值 / Log compaction threshold
# max_append_entries = 100      # 最大追加日志批量大小 / Maximum append entries batch size

# [migration]
# # 槽迁移配置 / Slot Migration Configuration
# batch_size = 100              # 迁移批量大小 / Migration batch size (keys per batch)
# concurrency = 4               # 迁移并发数 / Migration concurrency
# timeout = 300                 # 迁移超时时间（秒）/ Migration timeout in seconds
# rate_limit = 0                # 迁移限速 / Migration rate limit (keys per second, 0 = unlimited)

# [failover]
# # 故障转移配置 / Failover Configuration
# node_timeout = 15000          # 节点超时时间（毫秒）/ Node timeout in milliseconds
# replica_validity_factor = 10  # 副本有效性因子 / Replica validity factor
# failover_auth_timeout = 5000  # 故障转移授权超时 / Failover auth timeout in milliseconds
# require_majority = true       # 是否要求多数同意故障转移 / Require majority for failover

# [persistence]
# # 持久化配置 / Persistence Configuration
# enabled = true                # 是否启用持久化 / Enable persistence
# mode = "hybrid"               # 持久化模式 / Persistence mode
# rdb_interval = 900            # RDB 快照间隔（秒）/ RDB snapshot interval in seconds
# aof_sync = "everysec"         # AOF 同步策略 / AOF sync policy

# [performance]
# # 性能配置 / Performance Configuration
# worker_threads = 0            # 工作线程数（0 = CPU 核心数）/ Worker threads
# tcp_nodelay = true            # 是否启用 TCP_NODELAY
# tcp_keepalive = true          # 是否启用 SO_KEEPALIVE
# lua_timeout = 5000            # Lua 脚本超时时间（毫秒）/ Lua script timeout

# [monitoring]
# # 监控配置 / Monitoring Configuration
# prometheus_enabled = false    # 是否启用 Prometheus 指标 / Enable Prometheus metrics
# prometheus_port = 9100        # Prometheus 指标端口 / Prometheus metrics port
# slowlog_enabled = true        # 是否启用慢日志 / Enable slow log
# slowlog_threshold = 10000     # 慢日志阈值（微秒）/ Slow log threshold in microseconds
# slowlog_max_len = 128         # 慢日志最大条目数 / Maximum slow log entries

# [security]
# # 安全配置 / Security Configuration
# # requirepass = "your_cluster_password"       # 密码认证 / Password authentication
# # cluster_auth = "your_cluster_auth_key"      # 集群内部通信密钥 / Cluster auth key
# # tls_enabled = false                          # 是否启用 TLS / Enable TLS
# # tls_cert_file = "./certs/server.crt"
# # tls_key_file = "./certs/server.key"
